{
  "name": "logistics-railway-postgres-app",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack logistics app with React, Express, Prisma, PostgreSQL, ready for Railway deployment.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server": "node server.js",
    "build": "vite build",
    "start": "node server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:push": "prisma db push",
    "prisma:seed": "node prisma/seed.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "prisma": "^5.19.1",
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=18"
  }
}
